[Unit]
Description=PMG Quarantine Junk — action server
Documentation=https://github.com/mapazzzm/pmg-quarantine-junk
After=network.target postgresql.service
Wants=network.target

[Service]
Type=simple
User=pmg-quarantine
Group=pmg-quarantine
ExecStartPre=+/bin/sh -c 'touch /var/log/pmg-quarantine-junk.log && chown pmg-quarantine:pmg-quarantine /var/log/pmg-quarantine-junk.log && chmod 640 /var/log/pmg-quarantine-junk.log'
ExecStart=/usr/local/bin/pmg-quarantine-action-server
Restart=on-failure
RestartSec=10

StandardOutput=journal
StandardError=journal
SyslogIdentifier=pmg-quarantine-action-server

# Ограничения безопасности
PrivateTmp=true
ProtectSystem=strict
ReadWritePaths=/var/lib/pmg-quarantine-junk /var/log /run /tmp
WorkingDirectory=/tmp
ReadOnlyPaths=/etc/pmg-quarantine-junk /etc/pmg /usr/local/lib/pmg-quarantine-junk

[Install]
WantedBy=multi-user.target
